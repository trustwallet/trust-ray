/** ***************************************************************************
 * @file token.controller.js
 * @author Philipp Rieger
 *
 * @summary Provides CRUD operations for the token resource endpoint.
 * ****************************************************************************/


// load required modules
var mongoose = require('mongoose');
var Token = mongoose.model('Token');


/**
 * Reads one token.
 * @param req
 * @param res
 */
module.exports.readOneToken = function(req, res) {
};

/**
 * Reads all tokens.
 * @param req
 * @param res
 */
module.exports.readAllTokens = function(req, res) {
};